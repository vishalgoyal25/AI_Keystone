# Phase 0 · Evidence

Artifacts that prove the exit criteria were met — screenshots, charts, recordings — referenced
by [`../writeup.md`](../writeup.md).

Expected for Phase 0:
- Screenshot of the active AWS budget alarm
- CI run showing green on lint/format/docs checks
- `git check-ignore -v CLAUDE.md` output confirming the private files are ignored
